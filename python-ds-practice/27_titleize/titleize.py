def titleize(phrase):
    """Return phrase in title case (each word capitalized).

        >>> titleize('this is awesome')
        'This Is Awesome'

        >>> titleize('oNLy cAPITALIZe fIRSt')
        'Only Capitalize First'
    """
    # phrase.lower()

    # answer = phrase.split(" ")

    # final = ""

    # for each in answer:
    #     final = final + " " + each.replace(each[0], each[0].upper(), 1)
    
        
    return phrase.title()